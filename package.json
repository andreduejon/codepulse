{
	"name": "@andreduejon/codepulse",
	"version": "0.6.1",
	"description": "A terminal git graph visualizer that is read-only by default",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "https://github.com/andreduejon/codepulse.git"
	},
	"keywords": [
		"git",
		"graph",
		"tui",
		"terminal",
		"visualizer",
		"bun",
		"solidjs"
	],
	"engines": {
		"bun": ">=1.0.0"
	},
	"type": "module",
	"bin": {
		"codepulse": "./src/index.tsx"
	},
	"files": [
		"src/",
		"docs/",
		"tsconfig.json",
		"bunfig.toml",
		"LICENSE",
		"README.md"
	],
	"scripts": {
		"start": "bun run src/index.tsx",
		"test": "bun test",
		"lint": "biome lint src/ tests/",
		"format": "biome format src/ tests/",
		"check": "biome check src/ tests/",
		"check:types": "tsc -p tsconfig.json --noEmit && tsc -p tests/tsconfig.json --noEmit",
		"check:fix": "biome check --write src/ tests/"
	},
	"dependencies": {
		"@opentui/core": "0.5.6",
		"@opentui/solid": "0.5.6",
		"solid-js": "1.9.12"
	},
	"devDependencies": {
		"@biomejs/biome": "2.5.9",
		"@types/bun": "1.4.0",
		"typescript": "7.0.2"
	}
}
